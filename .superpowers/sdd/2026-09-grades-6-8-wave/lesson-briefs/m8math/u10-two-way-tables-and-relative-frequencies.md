# Lesson brief — m8math · Unit 10 Topic 4 · Two-Way Tables & Relative Frequencies

You are authoring ONE lesson-plan seed file. Follow the fan-out contract
exactly: `.superpowers/sdd/2026-09-grades-6-8-wave/m8math-FANOUT-CONTRACT.md`.
Read it in full before you write anything. It carries the segment recipe, the
lint requirements, the TypeScript shape and the hard rules.

## Your lesson — these values are authoritative, use them verbatim

| Field | Value |
|---|---|
| Unit | 10 — Bivariate Data: Scatter Plots & Two-Way Tables |
| Topic index in unit | 4 |
| Title | Two-Way Tables & Relative Frequencies |
| Slug | `two-way-tables-and-relative-frequencies` |
| Standard | `8.SP.A.4` |
| Seed file | `apps/tutor/src/lib/tutor/lesson-plan/seeds/m8math-u10-two-way-tables-and-relative-frequencies.ts` |
| Export symbol | `SEED_M8MATH_U10_TWO_WAY_TABLES_AND_RELATIVE_FREQUENCIES` |
| Plan id | `evelyn.ms.m8math.two-way-tables-and-relative-frequencies.v1` |
| Learning-objective id | `m8math.two-way-tables-and-relative-frequencies` |
| `los[0].standard` | `M8MATH-10.4` |
| `cedUnit` / `cedTopic` / `cedTitle` | `'10'` / `'10.4'` / `'Two-Way Tables & Relative Frequencies'` |
| `prerequisites` | ["m8math.using-a-linear-model-with-bivariate-data"] |
| `followUps` | [] |

## Scope — teach exactly this, and nothing adjacent

Organize bivariate categorical data in a two-way frequency table, compute row and column relative frequencies, and use them to describe a possible association (do students with a curfew tend to have chores?); numeric item asks for a cell frequency or a relative frequency. Withholds: conditional-probability language and independence tests — no shipped HS seed covers two-way tables (verified against all 40 `alg1-*` and 40 `geom-*` LO descriptions), so nothing is handed up from this row.

The neighbouring topics in this course own their own lessons; do not teach
theirs. The signed curriculum
(`.superpowers/sdd/2026-09-grades-6-8-wave/m8math-CURRICULUM.md`) lists every
sibling topic and an "Explicitly excluded" section naming what belongs to the
next grade — read both and stay inside your row.

## Salvage

none

## Hard rules

- Write ONLY `m8math-u10-two-way-tables-and-relative-frequencies.ts`. Do not touch `store.ts`, do not register anything, do not
  commit, do not push, do not run a deploy, do not touch a database, do not
  dispatch subagents. The controller registers all lessons in one batched edit.
- Every number must be arithmetically correct. Check each worked example and
  each answer by hand before you report.
- Age-appropriate for thirteen- and fourteen-year-olds (Grade 8).
- Report back: the file you wrote, and any place the contract or this brief was
  wrong or ambiguous. Do not paste the file's contents into your reply.
