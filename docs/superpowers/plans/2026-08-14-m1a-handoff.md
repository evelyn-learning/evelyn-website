# M1a Workspace Split — Handoff (Tasks 1–6 complete, 7–8 not started)

Branch: `m1a-workspace-split` @ `36754bd9`, branched from `main` @ `034f151a`.
Plan: `2026-08-14-m1a-engine-workspace-split.md`. Executed 2026-08-14.

**Scope executed: Tasks 1–6 only.** Tasks 7 (nginx / pm2 / deploy scripts / production
cutover) and 8 (CI boundary check, docs) were deliberately not started — they need a
scheduled window and explicit consent.

## State

10 commits. 5458 files changed, of which **5417 are pure renames** — 0 deletes, 41 creates.
`git mv` history survives throughout.

| Gate | Result |
|---|---|
| Regression oracle (181 hermetic scripts) | **178 pass / 3 red**, mechanically identical to `docs/superpowers/baselines/2026-08-14-pre-split.txt`. The 3 red (`test:verdict-guard`, `test:pedagogy-posed-problem` 8p/1f, `test:pedagogy-d1` 19p/2f) were red before this work and are unchanged. |
| Boundary invariant 1 | `grep -rn "@/lib/tutor\|@/app/tutor" apps/marketing/src` → **empty** |
| Boundary invariant 2 | `grep -rn "apps/marketing" apps/tutor/src apps/tutor/scripts` → **empty** |
| Typecheck | `apps/tutor` 0 errors, `apps/marketing` 0 errors |
| `npm ci --dry-run` | exit 0 |
| Builds | tutor 92 routes, marketing 581 routes, both succeed |

Because the test scripts are now split across two manifests, the oracle is the **union**:
`npm run test:all` (tutor, 172 scripts) + marketing's 9 `test:outreach-*` = the 181 baseline
entries. Any future check must compare the union, not tutor's `test:all` alone.

## Build times (all measured cold)

| | Before | After |
|---|---|---|
| Full build | 3:06.68 | — |
| `apps/tutor` alone | — | **2:31.77** |
| `apps/marketing` alone | — | **37.4s** |

The plan's acceptance gate #3 ("the tutor build is **materially faster** … if it isn't,
something is still cross-linked") is **not met as literally worded** — the tutor is only
~17% faster. But the gate's diagnostic premise is disproven: both boundary greps are empty,
marketing compiles in ~10s so it demonstrably carries no tutor code, and 155s + 37s ≈ 192s
against 186s pre-split, i.e. the split is almost exactly additive. Nothing is cross-linked.

The cause is code volume, not page count: `apps/tutor/src` is 3,141 files / 488,835 lines
versus marketing's 562 files / 117,556 lines. The pre-split build was already ~83% tutor, so
isolating the tutor could never have won big.

**Honest headline: marketing now deploys alone in 38s instead of 3:07 — a ~5× win on the app
that is deployed most often — and the tutor deploys alone in 2:32 instead of 3:07.**
Gate #3 should be amended to say this rather than reported as met.

## ⚠️ The branch is NOT deployable until Task 7

Root `npm run build` no longer exists (replaced by `build:marketing` / `build:tutor`), and
`deploy.sh`, `deploy-update.sh` and `deploy-to-production.sh` all still invoke it. Merging to
`main` without Task 7 in the same window leaves `main` un-shippable.

---

## Task 7 checklist — cross-process couplings

Post-cutover the two apps are separate processes behind nginx. Everything below is code in
one app reaching a path served by the other. **None of it is detectable by typecheck, tests,
or builds** — each becomes a production 404 unless nginx path-routing covers it.

### Relative API calls that cross the boundary

| # | Caller | Calls | Route lives in |
|---|---|---|---|
| 1 | `packages/core/src/hooks/useDemoTracking.ts:100` (used by `apps/tutor/src/app/tutor/page.tsx:180`) | `POST /api/demos/session` | **marketing** |
| 2 | `packages/core/src/hooks/useDemoTracking.ts:191` (same consumer) | `POST /api/demos/track` | **marketing** |
| 3 | `apps/marketing/src/components/demos/VoiceTutorLiveDemo.tsx:103` | `POST /api/tutor-portal/demo-token` | **tutor** |
| 4 | `apps/marketing/src/components/demos/HomeworkHelpBot.tsx:266` | `POST /api/tutor/extract-homework` | **tutor** |
| 5 | `apps/tutor/src/app/admin/video-curator/page.tsx:4` (`useSession`), via `apps/tutor/src/app/admin/layout.tsx:16` | `GET /api/auth/session` | **marketing** — NextAuth's only route is `apps/marketing/src/app/api/auth/[...nextauth]/route.ts` |

Item 5 matters: 12 tutor server gates use `getServerSession` and work fine (JWT cookie +
shared `NEXTAUTH_SECRET`), but the **client** `useSession()` on `/admin/video-curator` will be
permanently unauthenticated without an `/api/auth/*` rule.

### Cross-app iframes

| # | Consumer | Embeds |
|---|---|---|
| 6 | `apps/marketing/src/components/demos/VoiceTutorLiveDemo.tsx:261` | `/tutor-portal/embed?token=…` — page in **tutor** |

(`/ketcher/index.html` was resolved: `public/ketcher/` moved to `apps/tutor/public/`, so
`MoleculeRenderer.tsx:157` and the asset are in the same app.)

### Routes that changed owning process — nginx must follow

- `/admin/demos` + `/api/admin/demos/**` → **tutor**
- `/admin/video-curator` + `/api/admin/video-curator/**` → **tutor**
- `/admin/tutor-sessions/**` → **tutor**
- `/admin`, `/admin/login`, everything else under `/admin` → **marketing**
  (`apps/marketing/src/app/admin/page.tsx:205` links to `/admin/demos`, now tutor; the tutor
  admin pages `redirect('/admin/login')` and link `/admin`, now marketing)
- All 23 `/api/portal/v1/**` partner routes are in **tutor** — verified single-app, no split.
  Crimsora's contract is safe provided the whole prefix routes to the tutor upstream.
- `tutor.evelynlearning.com` / `tutor-sandbox.evelynlearning.com` must reach **tutor** — the
  host-rewrite `middleware.ts` now exists only there.

### Cross-app page links from tutor

Client-router navigations that will RSC-404 against the tutor origin before falling back to a
hard nav: `packages/core/src/components/layout/Header.tsx:87,151` (`/`, `/contact`);
`apps/tutor/src/app/tutor/page.tsx:2139,3158,3310` (`/products/voice-tutor`, `/contact`, `/`);
`apps/tutor/src/app/tutor-portal/components/PortalNav.tsx:29` (`/`).

### Process environment

Both apps now need **`.env` and `.env.local` in their own directory** on the server.
`apps/tutor/.env` and `apps/marketing/.env` are tracked; the `.env.local` files are local
symlinks to the repo root and must be recreated on the server. Dozens of ops scripts in both
apps load `path.join(__dirname,'..','.env.local')`.

The instrumentation split is clean and no cron is registered twice — tutor owns
`ENABLE_LEARNER_MODEL_SNAPSHOT`, marketing owns `ENABLE_BLOG_SCHEDULER`,
`ENABLE_OUTREACH_WATCHER`, `ENABLE_LEAD_RESEARCH` — but both processes read the same file, so
flags must be set for both.

---

## Deferred, with triage

**Before the Task 7 cutover**
- Everything in the checklist above.
- Rewrite the three deploy scripts (Task 7 replaces them with `deploy-tutor.sh` /
  `deploy-marketing.sh`; `deploy-update.sh` remains banned).

**Task 8 / housekeeping**
- Root `tsconfig.json` is dead and actively misleading: `include: ["scripts/**/*.ts"]` matches
  nothing (`npx tsc -p tsconfig.json` → TS18003) and its `"@/*": ["./apps/marketing/src/*"]`
  means any tool run from the repo root resolves **tutor** `@/` imports into **marketing**.
  39 scripts use `@/` or `@core/`; they work only because npm sets cwd to the app dir.
  Delete it or point it at both apps.
- `apps/marketing/src/components/layout/AppShell.tsx:22-37` still special-cases `/tutor-portal`
  paths marketing no longer serves — dead branch.
- Marketing's `experimental.middlewareClientMaxBodySize: '50mb'` is now inert (marketing has no
  middleware). Kept for constraint compliance.
- `packages/core/package.json` declares no `dependencies` while using `bcryptjs`, `mongoose`,
  `next-auth`, `jspdf`, `mammoth`, `pdf-parse`, `lucide-react`, `clsx`, `tailwind-merge`,
  `react`. Fine while deps are hoisted; brittle if that changes.
- `globals.css`, favicons and `global.d.ts` are now duplicated per app — silent-drift risk.
- Harness artifacts now land in `apps/tutor/artifacts/**` instead of `<repo>/artifacts/**`;
  pre-existing artifacts at the repo root are orphaned.
- `packages/core` holds two marketing-flavoured files (`components/layout/Header.tsx`,
  `data/solutions.ts`) alongside pure infrastructure. Both are genuinely dual-consumer, but it
  is an architectural call worth revisiting at M1e.

**Pre-existing bugs found, NOT introduced here, NOT fixed**
- **`/images/og-image.jpg` does not exist and never has.**
  `apps/marketing/src/app/layout.tsx:131,143` has referenced it all along, so
  evelynlearning.com's OpenGraph image has been 404ing since before this work. The tutor
  layout now mirrors the same metadata shape, so it inherits the same missing asset.
- **`npm run lint:passages` reports 74 "not public-domain" findings** on mock-exam passages.
  This check had been silently skipping (its data directory was guarded by `existsSync`, which
  this branch's data move restored); the findings are deterministic content debt, verified
  identical against pre-split `034f151a`.
- `WebPageJsonLd` in `apps/marketing/src/components/seo/JsonLd.tsx` has zero referrers.

**Known blind spot of this whole refactor.** Every codemod here anchored on import specifiers.
Three separate defect waves came from code reaching moved content by **filesystem path**
instead (`path.join(__dirname, …)`, bare CWD-relative strings, multi-line dynamic imports).
The oracle covers 181 of ~379 script files, so unwired ops tools are invisible to it. If
anything else surfaces, look there first.
