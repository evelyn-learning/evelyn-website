# R46 Micro-Round Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Single implementation task + battery — the four fixes are point changes too small for separate tasks.

**Goal:** Four point fixes from live session portal-0c48edbb: dangling-conjunction rail labels ("Limit laws and"), jump-verb misses ("let's get into X"), the brain wrapping to recap over a named jump request ("I'm done with this" misread as end-session), and the agenda strip's fat default scrollbar (`scrollbar-none` is an undefined phantom class).

## Global Constraints

- Base: branch `r46-fixes` off engine main @ 6fd50ac8, worktree `/Users/luke/Dev/evelynlearning/.claude/worktrees/rail-bargein`.
- The jump matcher's safety property stands: a verb alone must never fire without the ≥0.75 clear-winner label match; new verbs must be additions to `MOVE_VERB_RE`'s existing alternation idiom, prefix semantics unchanged.
- All existing suites stay green (known pre-existing: verdict-guard 1 failure; repo lint broken).

---

### Task 1: The four fixes

**Files:**
- Modify: `src/lib/tutor/lesson-plan/rail-labels.ts` (+ its harness `scripts/test-rail-labels.ts`)
- Modify: `src/lib/tutor/orchestrator/student-jump-intent.ts` (+ `scripts/test-student-jump-intent.ts`)
- Modify: `src/lib/tutor/ai/system-prompt-builder.ts` (+ the Rule-12 pin in `scripts/test-r33-prompt-rules.ts`)
- Modify: `src/app/tutor/components/VoiceTutorRealtime.tsx` (override telemetry)
- Modify: `src/app/globals.css` (define the missing scrollbar utility)

**(a) Label stopword-strip.** `capWords` (rail-labels.ts:28) cuts at `MAX_LABEL_WORDS = 3`, leaving dangling conjunctions ("Limit laws and"). After capping, strip trailing stopwords iteratively: `and, or, the, of, plus, with, vs, to, for, a, an` (case-insensitive) — "Cover the limit laws + direct substitution" capped to "Limit laws and" → "Limit laws". Apply the same strip wherever `capWords` output becomes a label (read both call sites — :29 and :35's 4-word LO fallback). Tests: the live shape → "Limit laws"; a label with no trailing stopword unchanged; a 2-word label unchanged; all 39 existing pins green (if an existing pin ENCODES a dangling-stopword label, update that pin and say so — the pin was pinning the bug).

**(b) Jump verbs.** Add to `MOVE_VERB_RE`: `get into`, `dig into`, `dive into` — as `(?:get|dig|dive)\s+into\s+` joined into the existing alternation (they carry their own preposition; do NOT route them through the `\s+(?:to|on to|onto|back to)\s+` tail). Live case pins: "Uh, yeah, let's get into the direct substitution examples." → matches the 'Direct substitution examples' item; "let's dig into quotient law restrictions" → matches; NEGATIVES: "let's get into it" (no label) → null; "I dig this topic" → null; all 31 existing pins green.

**(c) Named-destination-beats-wrap prompt rule + override telemetry.** (i) In system-prompt-builder.ts, inside Rule 12's block adjacent to the R44 named-jump HARD RULE, add (match the file's rule voice): `A destination named in the same utterance OVERRIDES closure phrases: "let's move to X — I'm done with this" means done with the CURRENT topic and asking for X — navigate to X. Never wrap up or advance to recap unless the student explicitly asks to end the session with no destination named.` Pin a distinctive phrase in the prompt harness. (ii) In VoiceTutorRealtime.tsx: the advance_lesson handler already clears `pendingStudentJumpRef` on entry — BEFORE clearing, when a pending exists AND the handler's resolved target differs from `pendingJump.segId`, emit `onDebugEvent?.('agenda_jump_overridden', `${pending.label} → ${pending.segId} vs brain → ${resolvedTarget}`)`. Read the handler to find where the target is resolved (resolveAdvanceTarget) relative to the clear — move the clear after resolution if needed, keeping semantics identical (still cleared unconditionally, regardless of resolution success; state the ordering in the report). `agenda_` prefix is already embed-whitelisted (verify by grep). No behavior change — telemetry only.

**(d) Scrollbar.** `AgendaRail.tsx:135` uses class `scrollbar-none` which is DEFINED NOWHERE (grep confirms) — the strip shows the default scrollbar. Define it in globals.css following the file's existing utility idiom (read how other custom utilities are declared there — Tailwind v4 `@utility` or a plain class): `scrollbar-width: none; -ms-overflow-style: none;` + `::-webkit-scrollbar { display: none }`. Check whether the VERTICAL rail branch and any other consumers use `scrollbar-none` too (grep) — defining the utility fixes all of them. NOTE the R35-era precedent: whiteboard cards deliberately show thin scrollbars on iOS (globals.css ~:214 comment) — scope the new utility to the class only, do not touch global scrollbar styling.

**Steps:**
- [ ] TDD where harnesses exist (a: rail harness; b: jump harness; c-prompt: pin harness) — failing first, then green.
- [ ] (c-ii) and (d) verified by `npx tsc --noEmit` + code reading (no component/CSS harness); report the hunks.
- [ ] Run: rail-labels, student-jump-intent, r33-prompt-rules, `npx tsc --noEmit`.
- [ ] Commit per fix or as one commit with a clear body — implementer's choice; report which.

### Task 2: Battery + build

- [ ] student-jump-intent, rail-labels, r33-prompt-rules, perception-classifier (109), utterance-answer-match (62), bargein-gate (45), cell-content (15), cover-layer, `npx tsc --noEmit`, `npm run build`.
- [ ] No merge/deploy — controller gates.
