# R45 Live-Test Fix Round Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the findings from live session portal-d7ec8e42 (2026-08-11): revert the false-positive-prone inference-based off-plan release (user decision), let the jump matcher work from an off-plan cursor, stop the agenda rail's auto-scroll from shifting the whiteboard, close the "shouldn't it be" hedge gap, fix factorial-list `$0!, 1!, 2!$` spans (spoken dollars + literal display), fix mixed prose/math table cells ("Beststrategyfor"), and stop the ack echo truncating fraction answers.

**Architecture:** Mostly reverts and targeted point fixes in existing modules; each task independently testable. The off-plan chip stays but only the brain's explicit `advance_lesson({to:"free"})` produces it.

**Tech Stack:** TypeScript, Next.js engine repo, `npx tsx scripts/test-*.ts` harnesses, `npx tsc --noEmit`.

## Global Constraints

- Base: branch `r45-fixes` off engine main @ 4bfb7d7f, worktree `/Users/luke/Dev/evelynlearning/.claude/worktrees/rail-bargein`. Line numbers below are approximate — locate by quoted code.
- USER DECISION 2026-08-11: the inference-based off-plan release is REVERTED (2/2 live firings were false positives — a student's tangent question is indistinguishable from the tutor going off-plan). The Off plan chip and the `to:"free"` cursor-release branch remain.
- The student-jump inference machinery (`pendingStudentJumpRef`, dispatch-time detection, seam apply, bracket-prefix gate, try/catch) is KEPT — do not remove it while removing the release machinery. Every removal task must list what it deliberately kept.
- Comparator conservatism unchanged: `unknown` fires nothing; hedge-stripping must never manufacture a false `disagree` (new hedges are prefix-anchored, like the existing ones).
- All existing suites stay green. Known pre-existing: test:verdict-guard 1 failure on main; repo lint broken.
- New/renamed debug events must be embed-whitelist-checked (grep `EMBED_DEBUG_EVENT_PREFIXES`).

---

### Task 1: Revert the inference-based off-plan release

**Files:**
- Modify: `src/app/tutor/components/VoiceTutorRealtime.tsx`

**What to REMOVE** (grep each symbol; remove declaration, writes, reads, and comments):
- `topicShiftForReleaseRef` — the ref, its sets at both topic-shift detection sites inside `runStudentTurnDetection`, its clears in the catch block / HTTP-failure early return / segment-card inferred-advance site / advance_lesson handler, and the seam's release branch (`agenda_offplan_release` emission and the copied cursor-release body inside the seam's `else if`).
- `turnFinishedSeqRef` — the counter, its three increment sites (seam, early return, catch), the `finishedAtDetection` capture, and the `.then` guard + `topic_shift_late_drop` emission (the embedding `.then` goes back to only setting `topicShiftPendingRef` for the newPage feature, exactly as before R44).
- The `advanceLessonCalledThisTurnRef`'s "navigated" tracking stays ONLY if something else still reads it — grep after the removals; if the seam's release branch was its only consumer, remove it too (including its reset site and the set in the advance_lesson handler and segment-card site); if the pending-jump seam logic reads it, keep it and say so.

**What to KEEP (verify each still present and functional after the removal):** `pendingStudentJumpRef` + its unconditional seam clear + catch/early-return clears + 90s staleness belt; dispatch-time `runStudentTurnDetection` invocation with the `!opts?.silent && !/^\s*\[/` gate and try/catch at BOTH sites; `agenda_jump_pending`/`agenda_jump_inferred`/`agenda_jump_stale` events; the seam's `applyResolvedAdvance(..., { seamMode: true })` apply; the Off plan chip rendering (rail-labels/AgendaRail untouched this task) and the `to === 'free'` release branch in the advance_lesson handler.

- [ ] **Step 1:** Grep-audit all symbols above; make the removals; `npx tsc --noEmit` clean (unused-symbol errors are the safety net — chase every one).
- [ ] **Step 2:** Run `npx tsx scripts/test-student-jump-intent.ts` (31) + `npx tsx scripts/test-rail-labels.ts` (39) — green.
- [ ] **Step 3:** Report: list every removed hunk (file:line, one-liner) + the keep-list verification + what happened to `advanceLessonCalledThisTurnRef`.
- [ ] **Step 4:** Commit — `git commit -m "revert(tutor): inference-based off-plan release — 2/2 live firings were FPs; chip stays for explicit to:'free'"`

---

### Task 2: Jump matcher works from an off-plan cursor

**Files:**
- Modify: `src/app/tutor/components/VoiceTutorRealtime.tsx` (detection gate inside `runStudentTurnDetection`; the seam apply condition)

Live failure: after an off-plan release, the cursor was `''` and the detection gate `if (lessonPlanRef.current && currentSegmentIdRef.current)` skipped the matcher — exactly when the student most needs "move to X" to work. (The release is reverted in Task 1, but `to:"free"` still produces an empty cursor, so this path remains reachable.)

- [ ] **Step 1:** Change the gate to require only the plan: `if (lessonPlanRef.current)`, passing `currentSegmentIdRef.current ?? ''` as the matcher's `currentSegmentId`. (`matchStudentJumpIntent`'s already-there guard does `segmentIds.includes(currentSegmentId)` — `''` never matches a real id, so all items stay eligible; no matcher change needed.)
- [ ] **Step 2:** Verify the seam apply handles `fromSegId === ''`: `applyResolvedAdvance`'s auto-complete loop no-ops when `outgoingIdx === -1` (read it; confirm), and the apply condition doesn't separately require a non-empty cursor — if it does, relax it to allow `''` → target.
- [ ] **Step 3:** `npx tsc --noEmit`; report a manual trace: cursor `''` (post-`to:'free'`) + "move to rule selection synthesis" → pending set → brain serves content w/o navigating → seam applies advance from `''` (no segments auto-marked) → pill lights.
- [ ] **Step 4:** Commit — `git commit -m "fix(tutor): student-jump matcher runs from an off-plan cursor"`

---

### Task 3: Container-scoped agenda-rail auto-scroll

**Files:**
- Modify: `src/app/tutor/components/session/AgendaRail.tsx`

Live failure: the active-pill auto-scroll uses `scrollIntoView`, which scrolls ANCESTOR containers too — moving the pill right shifted the whole whiteboard/stage left and never recovered (same class as the R35 transcript-drawer fix: "old scrollIntoView scrolled the parent marketing page"; that fix used container-scoped scrollTop math).

- [ ] **Step 1:** Read the component: the auto-scroll `useEffect` keyed on `currentKey` calls `currentRef.current?.scrollIntoView(...)` (~line 20-22). Replace with container-scoped math: add a ref on the rail's own scrollable element (the overflow container that holds the pills — identify it by its `overflow-x`/`overflow-y` class); in the effect compute the pill's offset relative to that container (`pill.offsetLeft`, `pill.offsetWidth` vs `container.scrollLeft`, `container.clientWidth` — or offsetTop/Height for the vertical orientation) and set `container.scrollLeft`/`scrollTop` directly (scroll only if the pill is outside the visible range; center it or scroll minimally — match the old `scrollIntoView` block/inline intent). NEVER call scrollIntoView. Handle both orientations if both render paths have the effect (read the whole file).
- [ ] **Step 2:** `npx tsc --noEmit` + `npx tsx scripts/test-rail-labels.ts` (pure model tests unaffected — confirm 39 green). No component harness: report the before/after effect code and a note on both orientations.
- [ ] **Step 3:** Commit — `git commit -m "fix(tutor): agenda-rail auto-scroll is container-scoped — no more whiteboard shift on pill change"`

---

### Task 4: Hedge-list extension + trailing-.0 numeric normalization

**Files:**
- Modify: `src/lib/tutor/voice/utterance-answer-match.ts`
- Test: `scripts/test-utterance-answer-match.ts` (extend; 50 existing stay green)

Two live misses in two sessions: "**Shouldn't it be** uh 9.0 e to the power of 3 x?" and "**it'll be just** 3 x squared e to the 3 x" both landed on `unknown` (hedges not in the strip list), so the inverse-verdict/praise-echo detectors couldn't judge them. Also "9.0" vs expected "9e^{3x}"'s coefficient 9: canonical forms differ on the trailing `.0`.

- [ ] **Step 1: Failing tests**

```ts
check('negative-question hedge', matchUtteranceToAnswer("shouldn't it be uh 9.0 e to the power of 3 x?", '9e^{3x}').verdict === 'agree');
check('contracted-future hedge', matchUtteranceToAnswer("it'll be just 3 x squared e to the 3 x", '3x^2e^{3x}').verdict === 'agree');
check('wouldnt-it hedge', matchUtteranceToAnswer("wouldn't it be 15?", '15').verdict === 'agree');
check('isnt-it hedge', matchUtteranceToAnswer("isn't it 3x + 2?", '3x+2').verdict === 'agree');
check('trailing .0 normalized', matchUtteranceToAnswer('9.0', '9').verdict === 'agree');
check('trailing .0 in expression', canonicalizeMathExpression('9.0e^(3x)') === canonicalizeMathExpression('9e^(3x)'));
check('non-trailing decimal untouched', matchUtteranceToAnswer('9.05', '9').verdict === 'disagree');
check('hedge does not flip a wrong answer', matchUtteranceToAnswer("shouldn't it be 7?", '9').verdict === 'disagree');
```

- [ ] **Step 2:** Implement: (a) extend `HEDGE_PREFIX_RE` with prefix-anchored alternatives: `shouldn'?t it be`, `wouldn'?t it be`, `won'?t it be`, `isn'?t it`, `couldn'?t it be`, `it'?ll be`, `it will be`, `it would be`, `that would be`, and allow `just` as a strippable connector in the loop (add `just` to the hedge alternatives so "it'll be just X" strips both). Keep everything prefix-anchored `^...` in the existing iterated-strip loop. (b) In `canonicalizeMathExpression`, after the whitespace strip, normalize trailing-zero decimals: `t = t.replace(/(\d+)\.0+(?![0-9])/g, '$1')` — placed so both the pure-number path and expression path see it; verify `extractAnswerNumber` already treats 9.0≡9 numerically (it does — parseFloat), so this only fixes the expression-path string compare.
- [ ] **Step 3:** All 50 existing + new green; `npx tsc --noEmit` clean. Also re-run `npx tsx scripts/test-praise-echo-check.ts` (16) + `npx tsx scripts/test-inverse-verdict-check.ts` (10) + `npx tsx scripts/test-perception-classifier.ts` (109) — comparator consumers must not flip.
- [ ] **Step 4:** Commit — `git commit -m "fix(tutor): hedge-strip covers negative-question and contracted-future forms; trailing-.0 decimals normalize"`

---

### Task 5: Factorial-list $-spans — TTS and display

**Files:**
- Modify: `src/lib/tutor/voice/tts-pronunciation.ts` (the span-walk's math/currency classification)
- Modify: the display mathifier (find `looksLikeMath` — grep `looksLikeMath` under `src/`; it moved from its old path)
- Tests: the math-coverage harness (`npm run test:math-coverage`, pins live in `scripts/test-math-coverage.ts`) + the inline-math/display suite that pins `looksLikeMath` (grep for its test file)

Live failure (session 15:49Z): the tutor SPOKE the dollar signs in "$0!, 1!, 2!$" and the transcript drawer showed the span literally. Root causes: (a) TTS — `MATH_SIGNAL_RE = /[\^_\\=]/` (~line 557) has no factorial signal, so `$0!` fell to the currency/artifact heuristics (leading `$digit` = price shape); the factorial verbalization rule (`(\d+|\b[a-zA-Z])!` → "N factorial", ~line 811) exists but the span never reached it as math. (b) Display — `looksLikeMath`'s bare-comma-list rule (R31) allows digits/commas but not `!`, so the span isn't recognized as math.

- [ ] **Step 1: Failing pins.** TTS: `"Does seeing it laid out as $0!, 1!, 2!$ next to $1, 1, 2$ make it click?"` must speak "zero factorial, one factorial, two factorial" (no "dollar"); `"$3!$"` → "3 factorial"; NEGATIVE: `"That costs $5. Try again!"` still speaks "5 dollars" (currency untouched); `"$0.5$"` unchanged. Display: `looksLikeMath('0!, 1!, 2!')` → true; `looksLikeMath('Great!')` → false; `looksLikeMath('Hello!, world')` → false.
- [ ] **Step 2: Implement.** TTS: extend the span math-signal detection with a digit-factorial test — `/\d!/` counts as a math signal for the $-span walk's classification (add to `MATH_SIGNAL_RE` as `|\d!` via a second regex or widen the artifact gate's pre-check — read how `MATH_SIGNAL_RE` is consumed at ~:725 and `isCurrencyPairingArtifact` ~:1209 first; the fix must make the factorial span classify as math BEFORE the currency shape test, without letting prose "Great! 5" through — the signal requires digit immediately before `!`). Display: extend the comma-list rule's charset to allow `!` immediately after a digit/letter (`0!, 1!, 2!` passes; `Great!` fails the list shape — it has no comma-separated structure; verify `Hello!, world` fails because `world` breaks the numeric-item rule).
- [ ] **Step 3:** Run the FULL math battery (`npm run test:math-coverage` — 171 pins + stress corpus MUST stay 0 failures) + the display suite + physics/chem/subject-notation quick check (`npm run test:physics`, `test:chem`, `test:subject-notation` — the span classification change touches shared machinery). `npx tsc --noEmit`.
- [ ] **Step 4:** Commit — `git commit -m "fix(tts+display): factorial lists in \$-spans are math — spoken 'n factorial', rendered KaTeX, never currency"`

---

### Task 6: Table cells — mixed prose/math renders with spaces and braces

**Files:**
- Modify: `src/app/tutor/components/whiteboard/CellContent.tsx`
- Test: grep for an existing renderer/table harness pinning CellContent (`grep -rl "CellContent" scripts/`); if none exists, create `scripts/test-cell-content-normalize.ts` pinning the pure normalization ONLY (extract the string-normalization steps into an exported pure helper so it's testable without React; the component keeps identical behavior).

Live failures (screenshots): header cell rendered "Beststrategyfor f^( n)(x)" (spaces eaten — whole cell went through KaTeX math mode; the docstring's prose-\text{}-auto-wrap didn't cover this shape) and "Productrule—butfor f^(n), considertheLeibnizrule". Also `f^(n)` renders as `f^(` + superscript-n glued (unbraced parenthesized superscript).

- [ ] **Step 1:** Read the FULL file (the $-span branch via InlineMathText at ~:48 is fine — these cells had NO $ delimiters and fell to the whole-cell path). Find why the prose auto-wrap failed for "Best strategy for f^(n)(x)" — likely the LaTeX-detection heuristic classified the whole cell as math (the `^` signal) and the \text{}-wrap step either doesn't exist on that path or its word-run regex fails on words adjacent to math tokens.
- [ ] **Step 2:** Implement, as an exported pure helper `normalizeCellForKatex(value: string): string` used by the component: (a) brace bare parenthesized superscripts: `\^\(([^()]{1,8})\)` → `^{($1)}` (fixes `f^(n)(x)`); (b) wrap prose word-runs (2+ consecutive alphabetic words separated by spaces, not preceded by `\`) in `\text{...}` preserving their spaces, leaving math tokens (`f^{(n)}(x)`, `e^{kx}`, `sin(kx)`) outside the wrap; em-dashes and punctuation stay inside the \text run. The three live cells are the fixtures: `"Best strategy for f^(n)(x)"` → renders "Best strategy for " upright + math; `"Product rule — but for f^(n), consider the Leibniz rule"`; `"e^{kx}, sin(kx), cos(kx)"` (already fine — must NOT regress: pure math cell stays unwrapped).
- [ ] **Step 3:** Pin the helper: the three fixtures assert on the normalized string (contains `\text{Best strategy for }`, contains `^{(n)}`, third is byte-unchanged). Run it + `npx tsc --noEmit`.
- [ ] **Step 4:** Commit — `git commit -m "fix(whiteboard): table cells wrap prose in \\text and brace f^(n) superscripts — no more glued words"`

---

### Task 7: Ack echo — fraction answers with trailing terms

**Files:**
- Modify: `src/lib/tutor/voice/cover-layer.ts` (`extractAnswerToken`)
- Test: the cover-layer harness (`npm run test:cover-layer`)

Live failure: student said "i over 1 + x"; the ack echoed "1 over 1" (the fraction capture stopped at the `+`, and ASR's "i"→"1" made it nonsense). Per R36's precedent (multi-number turns → null → generic ack), a fraction phrase followed by MORE math tokens should refuse rather than truncate.

- [ ] **Step 1: Failing tests** (harness idiom): `extractAnswerToken('1 over 1 + x')` → null (continuation after the fraction); `extractAnswerToken('minus 3 over 6')` → still the full spoken fraction (R36 pin — must not regress); `extractAnswerToken('1 over 2')` → unchanged capture.
- [ ] **Step 2:** Implement: after the fraction-expression capture, if the remainder of the utterance (beyond the captured token) still contains a math continuation (`plus|minus|times|over|divided|[+\-*/^]` followed by an operand), return null → generic non-echo cover line. Keep all existing captures.
- [ ] **Step 3:** Full cover-layer suite green; `npx tsc --noEmit`.
- [ ] **Step 4:** Commit — `git commit -m "fix(tutor): ack echo refuses truncated fraction answers — generic ack instead of '1 over 1'"`

---

### Task 8: Full battery gate

**Files:** none (verification only)

- [ ] **Step 1:** Run: student-jump-intent, rail-labels, bargein-gate, perception-classifier, utterance-answer-match, praise-echo, inverse-verdict, cover-layer, math-coverage (full: 171 + stress), physics, chem, subject-notation, r33-prompt-rules, mcq-letter-homophone, judge-correction-note. All green (verdict-guard's 1 pre-existing failure excepted).
- [ ] **Step 2:** `npx tsc --noEmit` + `npm run build`.
- [ ] **Step 3:** Report; do NOT merge/deploy — controller gates (headless-probe protocol applies at deploy).

## Self-Review (write time)

- Scope match vs user go: revert (T1) ✔, matcher-from-off-plan (T2) ✔, rail scroll (T3) ✔, hedges (T4) ✔, factorial spans (T5) ✔, table cells (T6) ✔, ack truncation ride-along (T7) ✔. "Uh, sure"-during-thinking deliberately deferred (needs instrumentation, not in scope).
- T1's keep-list protects the jump machinery from over-removal; tsc unused-symbol errors backstop the revert.
- T5 runs the full math battery + sibling notation suites because MATH_SIGNAL_RE-adjacent changes touch shared span classification.
