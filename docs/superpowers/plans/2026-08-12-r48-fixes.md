# R48 Implementation Plan — Rail Labels, Exercise Board Rule, Evidence Late-Fire

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Three items: (1) single-LO curated plans get stage-style rail labels instead of content-derived ones (phaseC verify round, session portal-1349716e, plan evelyn.ap.macro.scarcity.v1); (2) posed multi-part exercises must land on the whiteboard, not voice-only (same round, "her study of local water quality" delivered with nothing on the board) — prompt hard-rule + telemetry v1; (3) the evidence late-fire closing the same-turn affirm+complete → outcome 0.5 residual from R47 (the ftc-try class), under phaseC's shape-(a) constraints.

## Global Constraints

- Base: branch `r48-fixes` off engine main @ 8e6c21fb (R46+R47 merged), worktree `/Users/luke/Dev/evelynlearning/.claude/worktrees/rail-bargein`. Locate by symbol; VTR lines have shifted again post-merge.
- PHASEC CONSTRAINT LOCK (Task 3, binding — violation = STOP the task and report BLOCKED so the controller pings phaseC): shape (a) ONLY — hold the initial evidence emission for evaluative completions until post-stream credit resolution, emit once with the correct outcome; client-side only; the pagehide/keepalive flush MUST emit held outcomes with whatever credit exists at that moment (held rows are never dropped); NO changes to `src/lib/tutor/orchestrator/segment-evidence.ts` (resolver rules), NO changes to `src/lib/tutor/ai/completion-gate.ts` (recordMastery semantics), NO server/appendEvidence changes. `scripts/test-segment-evidence.ts` (31 cases) must stay green UNTOUCHED — the late-fire changes WHEN streakAtComplete is read, not the resolver's outcome table.
- Exercise-detection (Task 2) is telemetry-only in v1 — no kill, no retry; the prompt rule is the enforcement.
- All suites green; known pre-existing: verdict-guard 1 failure, lint broken.

---

### Task 1: Stage-style rail labels for single-LO plans

**Files:**
- Modify: `src/lib/tutor/lesson-plan/rail-labels.ts`
- Test: `scripts/test-rail-labels.ts` (43 pins post-R46 — all stay green; add new)

Live failure: curated single-LO plan rendered content-derived labels ("Wealth and scarcity", "Scarcity vs. poverty") — one pill per try-yourself question. Praveen's call: topic/stage-level names, never per-question content. Design (controller-decided): when the plan has ≤1 distinct LO, every rail item uses the KIND-DERIVED stage label machinery that already exists in this file (the `STAGE_WORD` map + `sameKind` numbering: Concept / Example / Try 1 / Try 2 / Misconception / Recap; hook/recap already fixed labels); multi-LO plans keep LO shortTitles exactly as today.

- [ ] Read the label-derivation flow (`buildRailModel`, the LO grouping, `loDisplay`, the content-derived path the live plan hit) and identify where the ≤1-distinct-LO branch belongs — count distinct LO ids across segments (state how a curated plan's segments carry their LO id; if the live plan's segments have NO lo ids at all, that IS the single-LO signal — handle both shapes).
- [ ] TDD: new pins reproducing the live plan's 8-segment shape (hook / concept / worked-example / try ×3 / misconception / recap) → labels exactly `Hook, Concept, Example, Try 1, Try 2, Try 3, Misconception, Recap`; a multi-LO fixture keeps LO shortTitles (pin unchanged behavior); a 2-LO plan is NOT single-LO (boundary pin). All 43 existing green.
- [ ] `npx tsc --noEmit`. Commit `fix(tutor): single-LO plans get stage-style rail labels — per-question content names retired`.

---

### Task 2: Posed exercises must reach the board — prompt rule + telemetry

**Files:**
- Modify: `src/lib/tutor/ai/system-prompt-builder.ts` (Rule-3a-sibling hard rule; pin in `scripts/test-r33-prompt-rules.ts`)
- Create: `src/lib/tutor/voice/exercise-board-check.ts` (pure detector + `scripts/test-exercise-board-check.ts`, register `test:exercise-board`)
- Modify: `src/app/tutor/components/VoiceTutorRealtime.tsx` (post-stream telemetry emit)

Live failure: "…tell me the job it's doing in three different sentences, starting with 'her study of local water quality.'" — a multi-part exercise, voice-only, board empty. Rule 3a already forces spoken FORMULAS onto the board; prose exercises slip through.

- [ ] **Prompt rule** (next to Rule 3a, matching its voice): posed exercise with concrete parts — enumerated items, (a)/(b)/(c), "N different sentences/examples/ways", a passage or quoted material to work from — MUST land on the board via show_problem (or a card) in the SAME turn it is spoken; voice-only exercises leave the student with nothing to look at. Pin a distinctive phrase.
- [ ] **Pure detector** `detectVoiceOnlyExercise(turnText: string): { posed: boolean; shape?: string }` — conservative shapes only: (i) `(a)`/`(b)` enumerations with an imperative/question; (ii) "N different/separate <sentences|examples|ways|cases>" with an ask verb (tell/give/write/come up with); (iii) quoted working material (a quoted phrase ≥ 4 words) + an ask verb + enumeration. NO match on rhetorical usage ("there are three different ways this shows up" — no ask verb + no second-person). Tests: the live utterance → posed (shape iii or ii); 6+ negatives (rhetorical counts, math turns with show_equation asks, plain questions).
- [ ] **Wire**: post-stream in `callBrainOnce` (beside the other post-stream advisories), when `detectVoiceOnlyExercise(fullText).posed` AND the turn dispatched ZERO board tool calls (read how the turn's tool names are tracked — `totalToolNamesSeen` or equivalent; count only board-rendering tools, not advance/mark): `onDebugEvent?.('exercise_no_board', shape + text slice)`. Telemetry only. Whitelist: check `EMBED_DEBUG_EVENT_PREFIXES` for coverage; add `'exercise_'` if needed.
- [ ] Suites: new harness + r33 pins + `npx tsc --noEmit`. Commit `feat(tutor): exercises must reach the board — prompt hard-rule + exercise_no_board telemetry`.

---

### Task 3: Evidence late-fire for same-turn affirm+complete (shape (a))

**Files:**
- Modify: `src/app/tutor/components/VoiceTutorRealtime.tsx` ONLY (see constraint lock — segment-evidence.ts, completion-gate.ts, and the server are untouchable; if the implementation cannot stay inside VTR wiring, STOP → BLOCKED)

The R47 residual: `mark_segment_complete` is handled MID-STREAM — it reads `demonstratedSegmentsRef`/`studentStreakRef` before the POST-STREAM pacing block credits the same turn's verified-correct answer, so the evidence row (insert-only idempotent at both layers) permanently records outcome 0.5. The booster already solves the analogous problem for the mastery flag via a post-stream late-fire.

- [ ] **Read first**: how the mark handler currently builds+pushes the evidence row (`resolveSegmentEvidence(...)` call site(s) in VTR — the explicit-complete path AND `applyResolvedAdvance`'s auto-visit path), where the accumulated rows live client-side (the segmentOutcomes/accumulator with its by-segmentId dedup), and where the pagehide/keepalive flush reads them.
- [ ] **Hold**: for the EXPLICIT-COMPLETE path only (auto-visit emissions unchanged — they carry no same-turn credit question), do NOT push the row at tool-call time. Instead stash a held record `{ segId, masteryDelta/note context, turnKey }` in a new ref (`heldEvidenceRef`). The dedup guard must still see the segment as "will be emitted" if that's needed to prevent a duplicate auto-visit row for the same segment — read the dedup flow and state how you kept exactly-one-row semantics.
- [ ] **Release at post-stream credit resolution**: in the pacing block, AFTER the credit decision applies (streak/demonstrated updated — including R47's objective-credit branch), resolve each held record by calling the SAME `resolveSegmentEvidence` inputs the mark handler would have used, but with the POST-credit streak/demonstrated values → push the row. Also release (with then-current values) on every non-completion exit: the catch block, the HTTP-failure early return, and the empty-turn fallbacks — a held row must never die with an errored turn (mirror where R44-47 rounds put their per-turn clears; emit there with pre-credit values, which is exactly today's behavior for those paths).
- [ ] **Flush safety**: find the pagehide/keepalive flush; before it serializes, drain `heldEvidenceRef` into the accumulator with whatever credit exists at that moment. Never drop a held row. State the ordering proof in the report.
- [ ] **Telemetry**: `onDebugEvent?.('evidence_late_fire', \`seg="${segId}" outcome=${outcome} streakAtComplete=${n}\`)` on each held-row release (prefix check: 'evidence_' — add to whitelist if uncovered).
- [ ] **Gates**: `npx tsx scripts/test-segment-evidence.ts` (31 — MUST pass with ZERO edits to that file); `npx tsx scripts/test-objective-credit.ts` (9); `npx tsc --noEmit`. Report: the hold/release/flush hunks + the ftc-try trace (verified-correct messy answer → arith kill credits post-stream → held row released with streakAtComplete ≥1 → outcome 1.0) + the error-path trace (turn errors → held row emitted with pre-credit values, byte-equivalent to today).
- [ ] Commit `feat(tutor): evidence late-fire — same-turn affirm+complete reads post-stream credit (shape (a), client-only)`.

---

### Task 4: Battery

- [ ] Run: test:rail-labels, test:exercise-board, test-segment-evidence (31, untouched), test:objective-credit, test-r33-prompt-rules, perception-classifier, utterance-answer-match, student-jump-intent, qpin, statement-dollars, tts-pronunciation, math-coverage (full), `npx tsc --noEmit`, `npm run build`.
- [ ] No merge/deploy — controller gates.
