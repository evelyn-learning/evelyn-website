# SDD ledger — holistic-pedagogy Plan 1 (engine) — copied verbatim from the git-ignored workspace at HEAD 2ff1c8a6 (includes Task 23 probes)

# SDD ledger — plan: docs/superpowers/plans/2026-09-05-holistic-pedagogy-plan1-engine.md
Spec: docs/superpowers/specs/2026-09-05-tutor-holistic-pedagogy-round-design.md (read). Base at start: 1aeba58c (== origin/main b1dace0c + docs).

## Pre-flight conflict scan (2026-09-05)
| pair / task | produces vs consumes | finding |
|---|---|---|
| T1 flags ↔ T6,T8,T13,T18,T20 | 4 flag names | consistent |
| T2 ledger ↔ T6 | createLedger/applyLedgerEvent/markRecovered/prereqKey/LedgerDetection | consistent |
| T3 store ↔ T4 route ↔ T18 | RecordGapInput.recap {offered,outcome?}; recurrences; inferred | consistent |
| T3 profile.nextSessionIntent ↔ T4 ↔ T13 accum.nextTimeIntent ↔ T15 read | commit field `nextSessionIntent` | consistent |
| T4 practiceLocator/homeworkAcknowledged ↔ T12 ↔ T13/T20 | commit body fields | consistent |
| T5 tool `recurrence` ↔ T6 handler | c.recurrence | consistent |
| T6 recurrenceListenerRef/activeLedgerLoRef ↔ T18 | refs | consistent |
| T6 loForSegment | plan says reuse the segmentOutcomes LO lookup | FINDING: that lookup lives in `segment-evidence.ts` (`resolveSegmentEvidence`), not inline. `lessonPlanRef` DOES exist (VTR:2065). |
| T7 looksMonetary | `looksMonetary(text: string)` | consistent with plan usage |
| T9 store ↔ T12 ↔ T15 ↔ Plan 2 | findOpenAssignments/findAssignmentBySession/acknowledge | consistent |
| T10 resolve ↔ T12 assign.ts | resolveAssignmentItems(input, sources) | consistent; `noGen` stub shape flagged in-plan |
| T11 status ↔ T14/T15/T19 | HomeworkStatus.overall incl 'weak' | consistent |
| T15 renderer signature | object form replaces (los,gaps) | FINDING: `scripts/test-learner-model.ts:2538-2600` calls the positional form and `getLearnerContextBlock(studentId, planId)` at :2614-2713 |
| T15 route extras ↔ T20 | learnerExtras {recapCandidate,homework,nextSessionIntent} | consistent |
| T17 blocks ↔ T18 input fields | recapOffer/recapGo/recapWrap/recapReply | consistent |
| T19 imports in system-prompt-builder (client-reachable) | types from practice-assign/status + learner-model/recap-candidate | RISK: recap-candidate imports store.ts (mongoose) — must be `import type` only |
| each task self-consistency | tests vs code, files created vs touched | consistent |

Rulings (pre-flight):
- Ruling: T6 resolves segment→LO via `segment-evidence.ts`'s existing resolver when it exposes one, else the plan's `lessonPlanRef`-based fallback — why: one source of truth for LO attribution — cost if wrong: mis-attributed ledger events on multi-LO plans.
- Ruling: T15 keeps a thin `getLearnerContextBlock(profileId, lessonPlanId)` wrapper returning `.block` (partnerId 'evelyn', externalStudentId = profileId) for the existing DB-backed test, and updates the positional `renderLearnerContextBlock` calls in `scripts/test-learner-model.ts` to the object form — why: spec §C.4 changes the renderer; the DB test must keep measuring the join — cost if wrong: one stale test.
- Ruling: any import of `recap-candidate.ts` / `practice-assign/status.ts` from a client-reachable module (`system-prompt-builder.ts`, VTR) must be `import type` — why: recap-candidate imports the Mongoose-backed profile store — cost if wrong: client bundle build failure (caught by `npm run build`).

## Task log
Task 1: minor (deferred): implementer report miscounted prefixes (13 vs 14) — code correct.
Task 1: complete (commits 1aeba58c..e97cea79, review clean)
Task 2: review → Critical: module + test committed at worktree ROOT `src/…`/`scripts/…` instead of `apps/tutor/…`; registered npm script fails from apps/tutor. Minor (deferred): `recovered` reset on every event untested; events array unbounded.
Task 2: fix round 1/5 (1 addressed partially — files relocated into apps/tutor + 2 recovered-flag tests, 1 open — root copies still tracked at HEAD; commits 33651e8b..a0cd81f6)
Task 2: fix round 2/5 (1 addressed — root copies git-rm'd; commits a0cd81f6..000ae4b3; re-review clean)
Task 2: complete (commits e97cea79..000ae4b3, review clean after 2 fix rounds)
Lesson for dispatches: state the working directory as `apps/tutor` AND that brief paths are relative to it.
Ruling: implementers run STRICTLY serially from Task 4 on (reviewers may overlap) — why: the Task 2 fix (git mv/rm) and the Task 3 implementer collided in the shared worktree and orphaned Task 3's first commit (recovered by the agent) — cost if wrong: slower wall-clock, no correctness cost.
Task 3: minor (deferred): mergeRecap materialises an empty RecapRecord when a caller passes `{offered:0}` with no outcome — callers must omit `recap` on no-op turns.
Task 3: complete (commits 000ae4b3..8976839c, review clean)
Ruling: mergeRecap must count `accepts` ONLY on outcome 'accepted' (not on 'improved'/'still_struggling', which only update lastOutcome) — why: Task 18 records 'accepted' at reply time and 'improved'/'still_struggling' at return; a profile flush between the two would otherwise double-count one recap as two accepts — cost if wrong: inflated accept counters only. Carried into Task 18's dispatch (store.ts one-line change + test case).
Task 4: review → Important (plan-mandated): `recap.outcome` passed un-narrowed from client JSON into the store. Ruling: FIX — narrow to the four-value union (else undefined) at the route, matching the file's segmentOutcomes validation pattern — why: the brief's snippet omitted it; the route is the trust boundary for client-supplied enum-likes — cost if wrong: none (strictly tighter). Minor (deferred): nextSessionIntent survival through recordPlanContentSeen/upsertSessionMemory unverified (both spread profile).
Task 4: fix round 1 queued (waits for Task 5 implementer to finish — serial rule).
Task 4: fix round 1/5 (1 addressed — recap.outcome narrowed via RECAP_OUTCOMES set; commit ad725358..8dae13ac; re-review clean)
Task 4: complete (commits 8976839c..2e1743c3 + fix 8dae13ac, review clean after 1 fix round)
Task 5: minor (deferred): "same two pedagogy-d1 reds before/after" rests on the implementer's report (reviewer could not re-run).
Task 5: complete (commits 2e1743c3..ad725358, review clean)
Task 6: implementer deviations (accepted pending review): (a) `brainGapFirstDetection` guard so a first-time brain record_gap (weight 2 ⇒ immediate detection with `existing` just pushed) is NOT counted as a recurrence — plan defect; (b) `feedLedger(kind, explicitLoId?, explicitSegId?)` so streak sites attribute to the answered-on segment; (c) flush-safety fallback pushes kind 'prerequisite' for prereq: keys. LO resolution = the component's own `loGroupOf(segId)` validated against plan.los (segment-evidence's resolver gates on evaluative kinds and needs a caller-computed group id).
Task 6: deferred: the "I'm stuck" BUTTON marker suppresses cueMatch (pre-existing) so it feeds no `stuck_cue` — Ruling: fold into Task 18's dispatch as a one-line `feedLedger('stuck_cue')` on the I'm-stuck button marker path — why: the button is the clearest stuck signal we have — cost if wrong: one extra ledger point per click.
Task 6: review → Important 1: brain `recurrence:true` with no accumulator entry (post-flush) pushes `recurrences:1` AND feedLedger bumps it again ⇒ recurrences 2 + two gap_recurred events. Important 2 (plan-mandated): `stuck_cue` fed from the boredom-cue site ⇒ only "skip" ever reaches the ledger.
Ruling: Important 2 — feed `stuck_cue` from the existing `isHelpRequest` detection (~9317) AND from the I'm-stuck button marker path; the boredom-cue-site feed is removed — why: the brief anchored the wrong site; the help-request detector is the real stuck signal — cost if wrong: extra ledger points on help requests (weight 1, threshold 2 — a single help request never detects alone).
Ruling: ratify Task 6 deviation (c): minimal flush-safety entry uses kind 'prerequisite' for prereq: keys (Ruling 2's literal 'lo' would have committed a bogus LO gap).
Ruling: Minor 3 (markRecovered on every correct answer, not only at streak reset) — KEEP as is — why: 'recovered' means the student got the LO right after a detection, which is exactly what §B.5 reads at recap return.
Ruling: Minor 7 — filter the brain-supplied signals through the closed GapSignalCode set before the listener cast (fold into the fix). Minors 5/6 deferred to the final review.
Task 6: fix round 1 queued behind the Task 8 implementer (both edit VoiceTutorRealtime.tsx).
Task 7: review → Important 1 (plan-mandated): opener-only test kills same-sentence corrective openers ("Right, but check your sign — that is not twelve."). Important 2 (plan-mandated): a correct INTERMEDIATE-step answer vs the card's final key kills ("Right, now add one." after "twelve" when key=13); the header's "same-claim scoping is structural" claim is false for single-valued keys.
Ruling (narrows spec §D.2 for precision): (1) after the opener matches, the remainder of the SAME sentence containing DENIAL_RE or a contrast marker (but|though|however|actually|except) ⇒ ok. (2) KILL tier = (a) MCQ: utterance resolves to a letter (choices given) that disagrees with the key letter — always a final answer, covers live instance 3; or (b) value disagree ONLY when the caller passes `finalAnswerTurn: true`; a value disagree without that flag ⇒ `advisory_false_praise` (correction note). Task 8 passes `finalAnswerTurn` from the orchestrator's knowledge that the student is answering the posed card (fresh currentProblemRef / staged spoken answer, no sub-question since) or leaves it undefined — why: the previous round's two false kills came from exactly this key-vs-sub-step shape — cost if wrong: an instance-2-class value reversal airs once (the arithmetic guard already covers instance 2). Header comment corrected. Minors (dead lookahead; three tests passing for the wrong reason; untested try/catch) — fix the dead lookahead + make the three tests load-bearing in the same round; classifyNonAnswer defense deferred.
Task 7: fix round 1 queued behind the Task 8 implementer (serial rule).
Task 8: implementer deviations (pending review): `opts.bareDenialWidening` (default true; orchestrator passes false when the flag is off) so flag-off is byte-identical; extra debug prefix `false_praise_opener_correction_note_planted`; dash-form aside test added because the brief's aside never reaches the new branch. Flagged risk: `studentVal.endsWith(v)` looser than equality.
Task 8: review → Important 1: the bare-denial arm fires on value-free denials about OTHER things (hypotheticals, forward warnings, part-scoped denials like "Your sign on the second term isn't quite right"). Important 2: `studentVal.endsWith(v)` lets a DIFFERENT value fire ("119".endsWith("9")). Minor 3: `vs.`/`e.g.` split can turn a value-naming denial into a bare one. ⚠️ forward dep: once Task 7's fix lands, the call site must pass `finalAnswerTurn`.
Ruling: Task 8 fix = exclusions only — (i) extend DENIAL_ASIDE_RE with hypothetical/forward markers (if someone|you|a student; careful|watch out|on the next|common mistake|students often|would(n't) be) and PART-scoped denials (a denial sentence containing `(on|for|in|with) (the|your) <word>` before the denial marker); (ii) suffix match only on a token boundary (preceding char not a digit) — equality otherwise; (iii) abbreviation guard in splitSentences (vs.|e.g.|i.e.|approx.|etc.); (iv) wire `finalAnswerTurn: lastStudentVerificationRef.current?.isVerification === true && lastStudentVerificationRef.current.turn === pacingTurnCounterRef.current` per the reviewer's recommendation — why: all four are precision-only changes on a kill path — cost if wrong: a few true positives lost.
Ruling: accept Task 8 deviations (bareDenialWidening opt; extra debug prefix; dash-form aside test).
Sequencing: Task 7 fix (running) → Task 8 fix → Task 6 fix, serial (Tasks 6 and 8 both edit VoiceTutorRealtime.tsx).
Task 7: fix round 1/5 dispatched+committed (8bad0b4e..ccdb74c5: same-sentence contrast/denial ⇒ ok; MCQ/value kill split with finalAnswerTurn; minors) — re-review running. Note: the implementer's report shows no "Fix round 1" heading — re-reviewer to confirm test evidence.
Task 7: fix-round deviation: a trailing "?" on the same-sentence remainder also ⇒ ok (probe 3 needed it) — accepted pending re-review.
Task 8: fix round 1/5 dispatched+committed (ccdb74c5..8f2d88cd: exclusions, boundary suffix, abbreviation guard, finalAnswerTurn wiring) — re-review running.
Task 6: fix round 1/5 dispatched (double-count, stuck_cue from isHelpRequest + I'm-stuck button, signal filter) — running.
Task 7: fix round 1/5 re-review → 4 addressed, 1 partial (Close/Almost tests still trivially true), 1 NEW Important: the trailing-"?" self-correction signal exempts ANY sentence ending in "?" (e.g. "Right! Should we try another one?" after a wrong MCQ letter ⇒ ok) — bypasses the MCQ-always-kills guarantee. Minor/info: CONTRAST_MARKER exempts "Right — actually it's thirteen." (residual gap, follow-up); mcq reason-prefix coupling.
Ruling: round 2 — (1) the "?" signal is restricted to wh-questions (`\b(where|what|why|how|which|when)\b` in the remainder) and NEVER exempts the MCQ-letter kill branch (only DENIAL_RE / contrast markers may exempt it); (2) Close/Almost tests get a positive control ("Correct." matches) in the same assertion so they pin the alternation; (3) "Right — actually it's thirteen." recorded as a deferred follow-up (true value disclosed in the same breath; praise-contradiction can't see a single sentence) — why: precision on the MCQ tier is the whole point of §D — cost if wrong: Socratic non-wh questions after a wrong value fall to the finalAnswerTurn rule, which is the intended default.
Task 7: fix round 2 queued behind the Task 6 fix (serial rule).
Task 8: fix round 1/5 (4 addressed, 0 open; commits ccdb74c5..8f2d88cd; re-review clean)
Task 8: minor (deferred): `would(n't) be` clause in DENIAL_HYPOTHETICAL_RE is broad; call-site comment is multi-line.
Task 8: complete (commits 0def367b..8bad0b4e + fix 8f2d88cd, review clean after 1 fix round)
Task 6: fix round 1/5 committed (8f2d88cd..992e50c0) — re-review running.
Task 7: fix round 2/5 dispatched (wh-question-only "?" exemption, never on the MCQ branch; Close/Almost positive controls) — running.
Task 6: fix round 1/5 (3 addressed, 0 open; commits 8f2d88cd..992e50c0; re-review clean)
Task 6: minors (deferred): brain-declared recurrences record no LedgerState event; the two recurrence paths' debug `count` semantics differ; inferred observation counts wrongs session-wide while signals are since-last-detection.
Task 6: complete (commits 8dae13ac..19c7669b + fix 992e50c0, review clean after 1 fix round)
Task 7: fix round 2/5 committed (992e50c0..8cfae06e) — re-review running. Task 9 implementer dispatched (serial: no other implementer live).
Task 7: fix round 2/5 (3 addressed, 0 open; commits 992e50c0..8cfae06e; re-review clean)
Task 7: deferred follow-up: single-sentence walk-back "Right — actually it's thirteen." is exempted by the contrast marker and invisible to praise-contradiction (needs a later sentence). mcq reason-prefix coupling (`/^mcq\b/`) noted.
Task 7: complete (commits 19c7669b..0def367b + fixes ccdb74c5, 8cfae06e; review clean after 2 fix rounds)
Section D (Tasks 7-8) complete. Sections A (1-6) complete.
Task 9: committed 1d6244af — review running. Task 10 implementer dispatched.
Task 9: minor (deferred): models/index.ts export line uses single quotes vs the file's double quotes.
Task 9: complete (commits 8cfae06e..1d6244af, review clean)
Task 10: committed feb0a8b5 (no-generation: explicit PracticeGenSources stub, since omitted genSources defaults to the real Anthropic path) — review running. Task 11 implementer dispatched.
Task 10: minors (deferred): fallback path doubles source calls per shortfall LO; excludeIds >500 truncation and a partial third-LO share are untested (brief's test design).
Task 10: complete (commits 1d6244af..feb0a8b5, review clean)
Task 11: committed 785464f0 — review running. Task 12 implementer dispatched.
Task 11: minors (deferred): same-timestamp tie-break is first-encountered; a JSON round-tripped assignment (string assignedAt) would break `.getTime()` — fine for the lean-doc producer.
Task 11: complete (commits feb0a8b5..785464f0, review clean)
Task 12: committed 109b8e9b — review running. Task 13 implementer dispatched.
Task 12: review → Important 1 (plan-mandated): reason/locator/nextTimeIntent caps only at the direct route; the commit fallback path is uncapped and its synthesized reason can exceed 240 chars. Important 2 (plan-mandated): route has an `!auth.allow ⇒ 401` branch mock-review-context lacks.
Ruling: Important 1 — FIX: centralize the caps (reason 240, locator 80, nextTimeIntent 200, trimmed) inside `assignPractice` so both callers inherit them; add a comment at the fallback that the unique sessionId index is the incidental TOCTOU mitigation — why: one enforcement point for a shared helper — cost if wrong: none.
Ruling: Important 2 — NO CHANGE, parked: practice-assign is a WRITE path and mirrors the student-profile POST's stricter `!auth.allow ⇒ 401` (the parity target in the brief was for identity resolution + rejection-reason handling, not the allow branch); a tokenless write must not silently land under 'evelyn' — why: same reasoning the M1c review applied to student-profile POST — cost if wrong: a retail (tokenless) close_session_notes call would 401 once EMBED_TOKEN_ENFORCE=on; retail sessions already pass an embed token on that path today.
Task 12: minors (deferred): awaited fallback adds commit latency; redundant getLessonPlan/resolveProfileIdOrRaw lookups; no length cap on sessionId/studentId.
Task 12: fix round 1 queued behind the Task 13 implementer (serial rule).
Task 13: committed 6f4c2014 — review running. Task 12 fix round 1 dispatched (resume).
Task 13: implementer notes (pending review): Lever A filter is allow-by-absence (no list to edit); `assignedPractice` added to the orchestrator handle type; `accumEmpty` gained `&& !accum.nextTimeIntent`; tool+prompt not flag-gated (only the handler) — accepted: a flag-off brain sees a tool whose call is dropped, same as other silent tools.
Task 12: fix round 1/5 committed (6f4c2014..af8a09d7) — re-review running. Task 14 implementer dispatched.
Task 12: fix round 1/5 (1 addressed, 1 parked with ruling — auth branch; commits 6f4c2014..af8a09d7; re-review clean)
Task 12: complete (commits 785464f0..109b8e9b + fix af8a09d7, review clean after 1 fix round, 1 parked)
Task 14: committed 102f7994 — review running. Task 15 implementer dispatched (with the pre-flight ruling: keep a getLearnerContextBlock wrapper; update test-learner-model.ts positional calls; type-only imports from client-reachable modules).
Task 13: review → Approved; Important (plan-mandated): with TUTOR_CLOSE_NOTES=off the tool+prompt+`practice location for homework:` line stay live, so the tutor can promise homework the dropped handler never assigns.
Ruling: FIX in round 1 — (a) gate the `practiceLocator` transient line on `TUTOR_CLOSE_NOTES` at both VTR call sites (no locator named ⇒ the prompt's own "say nothing about homework" rule applies flag-off); (b) Minor 2: move the once-latch to the success/204 path so a transient failure allows a retry (upsert is sessionId-keyed, so no double-assign); (c) Minor 5: scope the `accumEmpty` intent term to the final commit; (d) Minor 6: delete the dead `assignmentMade` field; (e) locator gate on the SUMMARY surface: set `assignedPracticeRef`/render the /tutor Homework line only when `practiceLocator` is present, and use the locator in the copy (spec §C.6: no locator ⇒ never shown) — why: the kill switch must fail safe and the summary must obey the same gate as speech — cost if wrong: none (all tighter).
Task 13: minors (deferred): summary card ref-mutation race after paint; nextSessionIntent lost on tab-close (pre-existing keepalive shape); refs read before declaration; stale transient-context doc comment; upsertAssignment findOne+upsert non-atomic (Task 12 scope; unique index bounds it).
Task 13: fix round 1 queued behind the Task 15 implementer (serial rule).
Task 14: review → Important (plan-mandated): tier 1 returns the first plan LO of a not-`done` assignment without checking that LO's own `status` (a done LO beside an untouched one wins). Ruling: FIX — skip LOs with `status === 'done'` inside the loop (keep the overall-done skip), add a two-LO test (done + untouched ⇒ the untouched LO) — why: spec §B.7 wording is per-LO — cost if wrong: none. Minor (accepted, ruling): softness may read a stale/resolved gap's recap history for homework/review-due candidates — intentional: a decline is a decline regardless of gap decay; add a one-line comment.
Task 14: fix round 1 queued behind the Task 15 implementer (serial rule); order after Task 15: Task 13 fix → Task 14 fix → Task 16.
Task 15: committed 6aa6a081 — review running. Task 13 fix round 1 dispatched (resume).
Task 15: implementer notes (pending review): DB join typechecked but never executed (no local Mongo; refused to run fixture-writing sections against the shared prod mongod — correct); `trendOf` yields '→' on every LO without a 14-day snapshot (noisy; consider omitting trend when prior is undefined — fold into the fix round if the reviewer agrees); homework lines are uncapped.
Task 13: fix round 1/5 committed (6aa6a081..140adc24) — re-review running. Task 14 fix round 1 dispatched (resume).
Task 15: review → Approved; Important 1 (plan-mandated): the two-stage trim never loops, ignores homework/goal/intent lines, stage 2 truncates ANY line at " · ", and no test exercises it (big input lands ~2.1KB). Important 2: the "degrades line by line" comment is false — one try wraps every sub-read.
Ruling: FIX round 1 — (a) make the trim converge: clip observations → drop LO digests (LO lines only) → drop goal lines → drop all but the first homework line → finally hard-truncate before `</learner_context>` with an ellipsis line; add a load-bearing test that forces each stage (8 LOs × 3 digests + 5 homework lines + 2 goals) and asserts `≤ 2400`; (b) wrap the optional sub-reads (hints already safe; assignments+homework rows; snapshot; digest rows) in their own try/catch so LO standing survives a partial failure, and make the comment true; (c) Minor 6: omit `trend` when there is no prior snapshot value (no glyph) — why: an unbounded prompt block and a false robustness claim are both real; the glyph noise is free to fix — cost if wrong: none.
Ruling: the boot-extras key is `nextTimeIntent` (typed `LearnerContextExtras`); Task 19's `ContinuityInput.nextTimeIntent` and Task 20 consume that name — the plan text's `nextSessionIntent` in Ruling 5 was a typo.
Task 15: minors (deferred): `goals` pipe-split; duplicate `new URL`; homework evidence query unbounded by date; `ability: steady` default noise.
Task 15: fix round 1 queued behind the Task 14 fix (serial rule).
Task 14: fix round 1/5 committed (140adc24..80ff857f) — re-review running. Task 15 fix round 1 dispatched (resume).
Task 13: fix round 1/5 (5 addressed, 0 open; commits 6aa6a081..140adc24; re-review clean)
Task 13: complete (commits 109b8e9b..6f4c2014 + fix 140adc24, review clean after 1 fix round)
Task 14: fix round 1/5 (2 addressed, 0 open; commits 140adc24..80ff857f; re-review clean)
Task 14: complete (commits af8a09d7..102f7994 + fix 80ff857f, review clean after 1 fix round)
Task 15: fix round 1/5 committed (80ff857f..28aa190b) — re-review running. Task 16 implementer dispatched.
Task 15: fix note — the worst-case fixture lands in the hard-truncation stage (gap lines lost); homework remains the last-trimmed axis. Deferred: cap describeHomework's LO list / raise the cap — final review to weigh.
Task 16: committed 4f69c105 — review running. Task 17 implementer dispatched.
Task 15: fix round 1/5 (3 addressed, 0 open; commits 80ff857f..28aa190b; re-review clean)
Task 15: complete (commits 102f7994..6aa6a081 + fix 28aa190b, review clean after 1 fix round)
Task 16: review → Approved; Important (plan-mandated) ×2: first-clause anchoring accepts "yes but can we do it after this problem" / "sure, what's a recap?" / "okay so the answer is twelve"; FILLER strips "so" anywhere so "I don't think so" ⇒ unclear.
Ruling: FIX round 1 — (a) strip filler only when LEADING (repeatedly), never mid-string; (b) add decline shapes `i don'?t think so|not really|no need|i'?m ok(ay)? without`; (c) accept only when, after the accept token, the remainder has ≤ 5 words AND contains no deferral/contrast/question marker (`but|after|later|first|wait|what|why|how|\?`) and no digit/number word — otherwise `unclear` (a soft decline, the safe outcome); pin the three probes as `unclear` (the "yes but…" one may be `unclear`, never `accept`) and "I don't think so" as `decline` — why: a false accept launches a 3-minute detour; a false unclear costs one offer — cost if wrong: a few genuine accepts phrased at length become unclear.
Task 16: fix round 1 queued behind the Task 17 implementer (serial rule).
Task 17: committed e4fd8229 — review running. Task 16 fix round 1 dispatched (resume).
Task 17: review → Approved; Important (plan-mandated): `loTitle` interpolated unescaped into <recap_offer>/<recap_go> (a `</recap_offer>` inside a title could break the block; mirrors the pre-existing formLabel convention). Ruling: FIX — in the stream route sanitizer strip `<` and `>` (and collapse whitespace) from `loTitle` before the 120-char cap; one pinned route-level or formatter test that a title containing `</recap_offer>` renders without the tag — why: plan titles for freestyle plans are LLM-generated — cost if wrong: none.
Task 17: fix round 1 queued after the Task 16 fix; Task 18 follows (serial rule).
Task 16: fix round 1/5 committed (e4fd8229..8313e3af) — re-review running. Task 17 fix round 1 dispatched (resume).
Task 16: fix round 1/5 (2 addressed, 0 open; commits e4fd8229..8313e3af; re-review clean)
Task 16: complete (commits 28aa190b..4f69c105 + fix 8313e3af, review clean after 1 fix round)
Task 17: fix round 1/5 committed (8313e3af..136f2146) — re-review running. Task 18 implementer dispatched (with the mergeRecap accepts-only-on-'accepted' ruling).
Task 17: fix round 1/5 (1 addressed, 0 open; commits 8313e3af..136f2146; re-review clean)
Task 17: complete (commits 4f69c105..e4fd8229 + fix 136f2146, review clean after 1 fix round)
Task 18: committed 2d950732 — review running. Task 19 implementer dispatched.
Task 18: implementer concern — one-turn ref consumption + `turns` bump sit inside the validator-retry loop, so a killed attempt's retry omits the offer/go block and double-counts turns. Ruling (fold into the fix round if the reviewer agrees): snapshot the recap blocks BEFORE the retry loop, attach the same snapshot on every attempt of the turn, consume the one-turn refs and bump `turns` exactly once AFTER the turn's dispatch settles — why: a retry is the same student turn; the brain must still see the offer — cost if wrong: none. Deviations accepted: classification hoisted above the loop; `pendingRecapReplyRef` dropped (unused); `recapWrap: undefined as true | undefined`.
Task 19: committed 8123206e (clause reworded "got the homework done well" to satisfy the brief's own test) — review running. Task 20 implementer dispatched (any Task 18 fix round queues behind it — both edit VTR).
Task 19: review → Approved; Important (plan-mandated): homework+offer / intent+offer clauses say "in ONE sentence" and also "ask … then wait for their answer" — self-contradictory. Ruling: FIX — the check/intent is ONE sentence, then "in a second short sentence, ask whether…"; the ONE-continuity-move sentence stays; update the test regexes; Oxford-comma join for 3+ LO titles; deferred minors: quote nesting in the intent interpolation; untouched/partial/weak without a candidate untested — add those three cases in the same round.
Task 19: fix round 1 queued behind the Task 20 implementer (serial rule; Task 18's fix also pending its review).
Task 18: review → Needs fixes. Important 1: one-turn carriers consumed per validator ATTEMPT (killed attempt ⇒ offer never spoken but recorded pending; go lost; wrap dropped; turns over-counted). Important 2: prereq recurrence keys arm recaps and fabricate kind:'lo' gaps with loId 'prereq:…'. Important 3: return-time in-place overwrite of `g.recap` erases an unflushed 'accepted' under the new mergeRecap. Important 4: first-'pending' lookup mis-attributes consent when two entries are pending; consume block never re-checks activeRecapRef.
Ruling: FIX all four — (1) turn-scoped carriers computed before the retry loop (matches the file's studentMarksForTurn convention), refs nulled and `turns` bumped once per logical turn; (2) listener ignores `prereq:` keys (recaps are LO-scoped this round); (3) return time PUSHES a second `{offered:0, outcome}` entry instead of mutating; (4) listener bails when any entry is pending; consume block re-checks activeRecapRef/pending. Minors folded: onDebugEventRef in the effect; overrun relative to wrapNudgedAtTurn; comment on the ledger-off cross-flag; constants to module scope; comment that the return hook fires on any resolved advance (intended).
Task 20: committed 3945b408 — review running. Task 18 fix round 1 dispatched (resume). Task 19 fix queued behind it, then Task 21.
Task 20: implementer concern — `homeworkAckIdsRef` filled at BOOT whenever homework exists, so ineligible journeys (resume-live/new) acknowledge homework the tutor never mentioned. Ruling: fill `homeworkAckIdsRef` only at the directive-build site when the chosen continuity clause is the homework clause (`continuity !== null` with `learnerExtrasRef.current.homework.length`) — why: "acknowledged" must mean "the tutor spoke to it" or the homework line silently disappears next boot — cost if wrong: a homework line repeats one extra session. Fold into Task 20's fix round after its review. Note: no `Goal:` thread producer exists until Task 21.
Task 18: fix round 1/5 committed (3945b408..addc47ae) — re-review running. Task 19 fix round 1 dispatched (resume).
Task 20: review → Approved; Important (plan-mandated, confirms the standing ruling): boot-time `homeworkAckIdsRef` fill permanently acknowledges homework on journeys where the clause never renders (and when the pedagogy-opener flag is off). FIX round 1: fill at the seed site only when the chosen clause is the homework clause; arm `armSessionStartRecap` only after `baseDirective` is known to carry the clause (Minor 2); filter `|` out of goal notes before joining (Minor 3); `import type { LearnerContextExtras }` instead of the inline retype (Minor 4); comment on the deliberately-outside-deps reads (Minor 5).
Task 20: fix round 1 queued behind the Task 19 fix (serial rule); Task 21 after.
Task 18: fix round 1/5 (4 Important + 5 minors addressed, 0 open; commits 3945b408..addc47ae; re-review clean)
Task 18: complete (commits 136f2146..2d950732 + fix addc47ae, review clean after 1 fix round)
Ruling: session-result.ts (portal emit path) forwards no recap/recurrences/inferred to recordGap — LEAVE: the runtime's student-profile commit is the primary write and carries them; the portal emit merges by loId afterwards without touching those engine-only fields (SessionEmitGap has no such fields by design) — cost if wrong: none (fields absent ⇒ no-op in mergeRecap/recurrence math).
Task 18 deferred minors: armSessionStartRecap should also refuse when any entry is pending (fold into the Task 20 fix — it owns that call site); bracketed turns count toward the wrap budget; drop-path debug line lacks source=.
Task 19: fix round 1/5 committed (addc47ae..e19494ae) — re-review running. Task 20 fix round 1 dispatched (resume).
Task 19: fix round 1/5 (1 Important + 3 minors addressed, 0 open; commits addc47ae..e19494ae; re-review clean)
Task 19: complete (commits 2d950732..8123206e + fix e19494ae, review clean after 1 fix round)
Task 20: fix round 1/5 committed (e19494ae..fb5a4803) — re-review running. Task 21 implementer dispatched.
Task 20: fix round 1/5 (1 Important + 4 minors addressed, 0 open; commits e19494ae..fb5a4803; re-review clean)
Task 20: complete (commits 8123206e..3945b408 + fix fb5a4803, review clean after 1 fix round)
⚠️ PROD FINDING (out of plan scope, surfaced by Task 22's cmp): worktree `.env.local.production` (344 lines, Aug 23) is 21 lines BEHIND root (365 lines, Aug 31): missing TUTOR_DEMO_GATE_SECRET, TUTOR_DEMO_IP_ALLOWLIST, TUTOR_MODEL_BRAIN_FALLBACK{,_BASE_URL,_API_KEY} + DeepSeek notes. deploy-tutor.sh bakes+uploads the CWD file, and the box's /root/evelyn-tutor/apps/tutor/.env.local is 344 lines / 19854 bytes dated Sep 5 ⇒ the 2026-09-05 verdict-layer deploy shipped the stale env. Effect in prod NOW: demo gate falls back to the evelyn-marketing partner secret (still enforced); IP allowlist absent; DeepSeek brain FALLBACK absent (Sonnet-only). Praveen must be told.
Ruling: Task 22 = `cp root → worktree` first (root is canonical), THEN insert TUTOR_LEARNER_CONTEXT=on, THEN `cp worktree → root`, cmp identical — why: the next deploy from this worktree then restores the missing keys — cost if wrong: none (root is the file Praveen last edited).
Task 21: committed 760bd5cb — review running.
Task 21: minor (deferred): extractor intro still says "in three kinds:" before four bullets.
Task 21: complete (commits fb5a4803..760bd5cb, review clean)
Task 22: complete (untracked env files; worktree synced FROM root then TUTOR_LEARNER_CONTEXT=on inserted; both 368 lines, byte-identical; demo-gate + brain-fallback keys now present in the deploy source)
## Final whole-branch review (opus, 1aeba58c..760bd5cb) — WITH FIXES
Critical 1: recap bookkeeping entries ('Recap offered/helped this session.', flush-safety 'Recurred later…') ride recordGap ⇒ phantom candidate gaps on homework-weak/review-due LOs (parent-visible via /gaps) and observation clobbering ('most recent wins').
Important 2: commit body sends raw `practiceLocator` (not `locatorForPrompt`) ⇒ TUTOR_CLOSE_NOTES=off still writes locator-stamped auto-assignments. Important 3: homework fully attempted but `weak` yields no recap candidate. Important 4: ALL open assignments acknowledged when only homework[0] is spoken. Important 5: praise-contradiction kill reason nonsensical for the bare-praise branch. Important 6: "go straight in"/"sure why not"/"let's just start" classify unclear ⇒ declines never counted.
Rulings (ONE fix wave): (1) `bookkeepingOnly?: boolean` on accumulator entries + CommitBody + RecordGapInput; store merges recap/recurrences into an EXISTING active match only, never creates, never overwrites observation/signals/quotes; test:gaps case. (2) `locatorForPrompt` in the commit body. (3) recap-candidate skips per-LO 'done' only when `h.overall !== 'weak'`; test. (4) acknowledge only `homework[0].assignmentId`. (5) detectPraiseContradiction returns a `branch` discriminator; call site branches the reason. (6) recap-reply: unanchored straight-in / why-not / let's-just-start shapes; tests. Fold: T21 "in three kinds:" wording. Leave (recorded): MCQ kill ignores finalAnswerTurn (probe it); dead locator fallback copy; goals rendered twice; subject param gated on RECAP_OFFER; homework evidence query date-unbounded; packages/core additive change ⇒ deploy note.
Deferred/parked triage: all SHIP-AS-FOLLOW-UP; T6 stuck-button line closed (already wired).
Gate (pre-fix-wave, HEAD 760bd5cb): tsc 0; build clean (all four NEXT_PUBLIC flags present in 13 bundle files each — absent from env ⇒ ON, the proven-live pattern); test:all FAIL rows = test:embed-token, test:verdict-guard, plus the two pedagogy prompt-text reds (posed-problem 8/1, d1 19/2) — the four known pre-existing reds, nothing new. Fix wave dispatched (opus, one agent, findings 1-6 + T21 minor); gate to be RE-RUN after it.
Final fix wave: 6 commits 760bd5cb..d659a678 (bookkeeping-only gaps; weak homework candidate; contradiction branch discriminator; recap-reply shapes; three-kinds wording; orchestrator side of 1/2/4/5) — scoped re-review dispatched; agent report pending; gate re-run after.
Final fix wave re-review: all 7 findings ADDRESSED; batteries green; NEW Important: `feedLedger`/brain-recurrence treat a bookkeeping-only accumulator entry as `existing`, so a real first detection only bumps its `recurrences` (no inferred push) and the server no-ops ⇒ nothing recorded (regression vs pre-fix). Minor: route processes bookkeeping entries in array order (a later brain gap loses the earlier offer counters). Out-of-scope: `lastSeenAt`/sessionIds bump on bookkeeping merges defeats applyCrossSessionPromotion for that session; DECLINE_UNANCHORED_RE broadened beyond the ruled phrases; summary card gated on raw practiceLocator; positional LO pick on weak assignments; stale client bundles.
Ruling (deliberate deviation from "no second fix wave" — the wave itself introduced a default-path data-loss defect; deploy is Praveen-gated regardless): ONE scoped follow-up on the same agent: (a) exclude `bookkeepingOnly` entries from the `existing` lookups in feedLedger and the brain-recurrence path (let the inferred/brain push run beside them); (b) route loop processes non-bookkeeping entries first; (c) bookkeeping merges update ONLY `recurrenceCount`/`recap` — no `lastSeenAt` bump, no sessionIds add (adjust the test); (d) DECLINE_UNANCHORED_RE restricted to the four ruled phrases; (e) summary-card gate uses `locatorForPrompt`. Deferred: positional LO pick on weak assignments; stale-bundle transient.
Follow-up landed: caefdf04 (both accumulator lookups skip bookkeepingOnly; route stable partition; store bookkeeping merge = recurrenceCount+recap only; DECLINE_UNANCHORED_RE = four end-anchored shapes + AMBIGUOUS_START_RE ⇒ unclear; summary-card gate reads locatorForPrompt). Agent-reported: tsc 0; gaps 62, recap-reply 46, praise-contradiction 40, false-praise-opener 36; test:all 225 PASS / 4 known FAIL. Scoped re-review dispatched (sonnet) → re-review-followup-caefdf04.md; gate2 (tsc + test:all) running → gate2-*.log.
Ruling: route auto-assign fallback keeps bookkeeping entries as candidates (recurrences>=1) — why: such an entry exists only after a detection on that LO, so its counter is a real recurrence signal and the behaviour predates the wave — cost if wrong: one auto-assignment on an LO whose profile gap was already resolved (student sees one extra practice set). Parked.
Ruling: AMBIGUOUS_START_RE ('decline shape + tail' ⇒ unclear) ratified — why: the ruled pin was unreachable by narrowing alone (≤5-word accept remainder would have made it 'accept'); unclear makes the brain re-ask — cost if wrong: one extra clarifying question on a rare reply shape.
Scoped re-review of caefdf04: items 1,2,3,5 ADDRESSED; Important — end-anchored decline shapes lost to trailing punctuation ("let's just start." ⇒ unclear). Ruling: controller fixed directly (single-file mechanical): trailing [.,!?-] stripped in normalization + 3 pins; recap-reply 49/0; tsc 0 — commit b7c75c43. Pin "I'd rather go straight in, thanks" NOT added — a word tail is 'unclear' by the ruling.
Gate 2 (on caefdf04): tsc 0; test:all 225 PASS / FAIL = exactly the four known reds; build exit 0, all four flags inlined (2 chunk files each); BUILD_ID u2_SQPr-A9mo1sbEHC5bW (superseded by the recap-reply commit; deploy rebuilds).
Gate 3 (b7c75c43): test:all 225 PASS / FAIL = the 4 known reds.
Task 23 (Praveen: "Merge push deploy"): ./deploy-tutor.sh from the worktree exit 0 (log deploy-tutor-20260905T111843Z.log); prod BUILD_ID qJcURHZXRjDoeaumLK__y; pm2 evelyn-tutor online restarts=0 pm_uptime 1788607849056; :3007 /tutor 200; flags inlined in 2 prod chunk files; prod env /root/evelyn-tutor/apps/tutor/.env.local = 368 lines with TUTOR_LEARNER_CONTEXT=on + demo-gate secret + IP allowlist + DeepSeek fallback (drift CLOSED). Pushed tutor-rounds:main b1dace0c..e6ccdd24 (fast-forward). Remaining: typed-input probes + live voice verification.
Task 23 typed-input probes (dev :3007, real brain, harness dev hooks; prod build strips them): run 1 (accept a / decline b on d659..e6ccdd24 code): ledger chain PROVEN (gap_inferred → gap_recurred count=2 → recap_offer_armed source=recurrence → recap_offer_reply). DEFECTS: (1) brain never VOICED the offer in either run — the student's "I'm stuck" utterance triggers the stuck HARD RULE (one sub-question, nothing else) and the block lost; the next utterance was consumed as a phantom reply; (2) "yes please, a quick one" ⇒ unclear (number word "one" in the remainder); (3) goodbye turn spoke "That's session-end, no math needed here." and did NOT call close_session_notes (tool WAS sent — universal in the subject filter); (4) harness never ends the session so the final commit/auto-assign fallback was unexercised (profile gap recorded via periodic flush: inferred, recurrenceCount 1; 0 assignments).
Ruling: controller fixed directly (9a113ea1): isRecapOfferVoiced() at turn ok → unvoiced ⇒ withdraw pending + re-arm ≤3 attempts + `recap_offer_unvoiced`; block + stuck rule say the offer replaces the sub-question; chained accept tokens peeled; closing section forbids narrating the turn's kind; harness __tutorEndSession + endSession scenario option. Probe run 2 (ids c/d) launched.
Probe run 2 (9a113ea1): decline (d): offer VOICED ("want me to take a couple of minutes to recap…") → decline → no recap, no re-offer ✓. accept (c): recap_offer_unvoiced ×3 → dropped (brain kept answering the stuck cue with a sub-question; the guard correctly withdrew the offer so "yes please, a quick one" was NOT consumed as a phantom reply); goodbye → close_session_notes CALLED (assignLoIds, reason, nextTimeIntent) → practice_assigned silent=no-locator, 4 bank items; PracticeAssignment + profile.nextSessionIntent written via the End path ✓; no goodbye self-narration ✓. homework-check (c, session 2): GET extras with lessonPlanId+subject; assignment has NO locator ⇒ deliberately not announced (requireLocator, ruling §C.6); opener honoured nextTimeIntent instead ("Last time we got as far as … a=5 — let's finish it now") ✓. homework_checked with a locator needs an embed session (academy passes practice_locator) — Plan 2 territory; unit pins cover the clause.
Ruling: recap blocks moved to the HEAD of userContent + "PRIORITY THIS TURN" (commit after 9a113ea1) — why: 1/2 voicing compliance with the block buried; position is the cheapest lever — cost if wrong: none functional (re-arm guard bounds the damage).
Observations for Praveen (verdict-layer territory, not this round): spoken self-narration ×3 across probes — "That's session-end, no math needed here.", "That's a request, not an attempt at this one yet", "Let me support them concretely." (third person!). Harness artifact: opener says "First time chatting" on session 2 because e2e sessions skip the session store.
