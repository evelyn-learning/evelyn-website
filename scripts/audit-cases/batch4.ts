/**
 * Clip-audit cases — batch 4. Populated by the batch-4 fix agent: one case
 * per fixed label site, using the audit doc's worst-case payload, with
 * expectViolation:false once the fix lands.
 */
import type { AuditCase } from '../lib/svg-text-extents';

export const CASES: AuditCase[] = [];
