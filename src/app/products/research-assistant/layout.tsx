import { Metadata } from 'next';
import { SoftwareApplicationJsonLd, FAQPageJsonLd, BreadcrumbJsonLd } from '@/components/seo/JsonLd';
import { productFAQs } from '@/data/faqs/products';
import { getKeywordsForProduct } from '@/lib/seo/keywords';

const productId = 'research-assistant';
const productName = 'Research Assistant AI';
const productDescription = 'Literature search, paper summarization, citation management, and methodology suggestions for graduate students, faculty, and researchers. Accelerate academic research with AI assistance.';

export const metadata: Metadata = {
  title: `${productName} | Academic Research Tools`,
  description: productDescription,
  keywords: getKeywordsForProduct(productId),
  openGraph: {
    title: `${productName} | Evelyn Learning`,
    description: productDescription,
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  const faqs = productFAQs[productId] || [];

  return (
    <>
      <SoftwareApplicationJsonLd
        name={productName}
        description={productDescription}
        applicationCategory="Educational Software"
        url={`/products/${productId}`}
        featureList={[
          'Smart literature search',
          'Paper summarization',
          'Citation management',
          'Methodology guidance',
          'Literature review generation',
          'Reference manager export',
          '15+ citation formats',
          'Multi-database search',
        ]}
      />
      <FAQPageJsonLd faqs={faqs} />
      <BreadcrumbJsonLd
        items={[
          { name: 'Products', url: '/products' },
          { name: productName, url: `/products/${productId}` },
        ]}
      />
      {children}
    </>
  );
}
