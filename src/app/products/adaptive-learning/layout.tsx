import { Metadata } from 'next';
import { SoftwareApplicationJsonLd, FAQPageJsonLd, BreadcrumbJsonLd } from '@/components/seo/JsonLd';
import { productFAQs } from '@/data/faqs/products';
import { getKeywordsForProduct } from '@/lib/seo/keywords';

const productId = 'adaptive-learning';
const productName = 'Adaptive Learning Engine';
const productDescription = 'AI-powered personalized learning paths that adapt in real-time to each student\'s strengths, weaknesses, and learning pace. Intelligent skill mapping and mastery-based progression for optimal learning outcomes.';

export const metadata: Metadata = {
  title: `${productName} | AI-Powered Learning`,
  description: productDescription,
  keywords: getKeywordsForProduct(productId),
  openGraph: {
    title: `${productName} | Evelyn Learning`,
    description: productDescription,
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  const faqs = productFAQs[productId] || [];

  return (
    <>
      <SoftwareApplicationJsonLd
        name={productName}
        description={productDescription}
        applicationCategory="Educational Software"
        url={`/products/${productId}`}
        featureList={[
          'Skill mapping and visualization',
          'Real-time difficulty adaptation',
          'Knowledge graph integration',
          'Spaced repetition scheduling',
          'Mastery-based progression',
          'Learning analytics dashboard',
        ]}
      />
      <FAQPageJsonLd faqs={faqs} />
      <BreadcrumbJsonLd
        items={[
          { name: 'Products', url: '/products' },
          { name: productName, url: `/products/${productId}` },
        ]}
      />
      {children}
    </>
  );
}
