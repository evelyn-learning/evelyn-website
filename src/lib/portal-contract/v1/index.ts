/**
 * Portal Contract v1 — public barrel.
 *
 * Import from here:  `import { StudentContextSchema, type StudentContext } from '@/lib/portal-contract/v1';`
 *
 * See `schemas.ts` for the canonical zod validators and `types.ts` for the
 * derived TypeScript types + engine drift guards. Breaking change ⇒ add a
 * `v2/` sibling; v1 stays frozen.
 */

export * from './schemas';
export * from './types';
