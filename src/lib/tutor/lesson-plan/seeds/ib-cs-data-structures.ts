/**
 * IB Computer Science — Data Structures.
 */

import type { LessonPlan } from '../types';

export const SEED_IB_CS_DATA_STRUCTURES: LessonPlan = {
  id: 'evelyn.ibdp.cs.data-structures.v1',
  title: 'IB Computer Science — Data Structures',
  curriculum: 'IB-DP',
  grade: '11-12',
  subject: 'cs',
  topic: 'ib-cs',
  locale: 'en',
  los: [{ id: 'ibdp.cs.data-structures', description: 'Apply array, list, stack, queue, linked list, tree, hash table data structures and their tradeoffs.', standard: 'IB-DP-CS' }],
  prerequisites: [],
  followUps: [],
  estimatedMinutes: 16,
  segments: [
    { id: 'hook', kind: 'hook', goal: 'Right data structure = fast algorithm. Wrong data structure = slow algorithm.', script: 'Need fast lookup? Hash table. Need ordered traversal? Tree. Need FIFO behaviour? Queue. IB CS expects you to choose appropriately + analyse complexity.', estimatedMinutes: 1 },
    { id: 'concept', kind: 'concept', goal: 'Common structures + Big-O analysis.', keyIdeas: [
      'ARRAY: contiguous memory; fixed size (in most languages).',
      '  Access by index: O(1).',
      '  Insert at end: O(1) amortised. Insert in middle: O(n).',
      '  Search: O(n) unsorted, O(log n) if sorted (binary search).',
      'LINKED LIST: nodes with data + pointer to next.',
      '  Access by index: O(n).',
      '  Insert at head: O(1). At tail (with tail pointer): O(1).',
      '  Search: O(n).',
      '  Vs array: better for frequent insertions; worse for random access.',
      'STACK: LIFO (last in, first out).',
      '  Push, pop, peek: O(1).',
      '  Examples: function call stack, undo, depth-first search.',
      'QUEUE: FIFO (first in, first out).',
      '  Enqueue, dequeue: O(1).',
      '  Examples: print queue, breadth-first search, scheduling.',
      'TREE: hierarchical, nodes with children.',
      '  BINARY TREE: each node has up to 2 children.',
      '  BINARY SEARCH TREE (BST): left < node < right.',
      '    Insert/search/delete: O(log n) average, O(n) worst (unbalanced).',
      '    Self-balancing variants (AVL, red-black): guaranteed O(log n).',
      '  HEAP: complete tree where parent ≥ children (max heap) or ≤ (min heap).',
      '    Insert + extract-max: O(log n). Used for priority queues.',
      'HASH TABLE: array indexed by hash function of keys.',
      '  Insert/search/delete: O(1) average, O(n) worst (collisions).',
      '  Used everywhere: dictionaries, sets, caches, database indices.',
      'GRAPH: nodes + edges (directed or undirected).',
      '  Adjacency list (better for sparse graphs).',
      '  Adjacency matrix (better for dense graphs).',
      'CHOOSING the right structure:',
      '  Need fast lookup by key → HASH TABLE.',
      '  Need ordered traversal → BST or sorted array.',
      '  Need FIFO → QUEUE.',
      '  Need LIFO → STACK.',
      '  Need priority access → HEAP.',
    ], vocabulary: [{ term: 'Big O notation', definition: 'asymptotic upper bound on runtime; e.g., O(n) means linear time, O(log n) logarithmic.' }, { term: 'amortized', definition: 'average cost per operation across a sequence; for dynamic arrays, occasional resize is O(n) but amortizes to O(1) per insert.' }], estimatedMinutes: 5 },
    { id: 'worked', kind: 'worked_example', problem: 'You need to track recently-visited URLs in a browser. The number of URLs stored is fixed; oldest is dropped when new added. What data structure?', steps: [
      'Behavior needed: add to one end, remove from other end. FIFO.',
      '⟹ QUEUE (or specifically, a circular/bounded queue when capacity is fixed).',
      'Operations: enqueue (when visiting URL), dequeue (when capacity exceeded).',
      'Implementation choices: linked list (easy enqueue/dequeue) or fixed-size circular array (no allocation overhead).',
    ], answer: 'Queue (FIFO behavior; bounded with circular array if fixed size).', estimatedMinutes: 3 },
    { id: 'try-1', kind: 'try_yourself', problem: 'You\'re building a phonebook app. Names map to phone numbers. You expect frequent lookups. What data structure?', expectedAnswer: 'HASH TABLE (also called dictionary, map). Keys = names, values = phone numbers. Lookup is O(1) average — exactly what you want for frequent lookups. Tradeoff: slightly more memory than a sorted array; iteration order is not sorted (sorted array would be better if you needed alphabetical traversal often).', responseFormat: 'free', hints: ['What\'s the key access pattern?', 'Which data structure has O(1) lookup?'], estimatedMinutes: 3 },
    { id: 'misconception-array-vs-list', kind: 'misconception_check', question: 'A student says "linked lists are always faster than arrays." Why is this wrong?', commonErrors: [{ answer: 'Linked lists faster than arrays', misconception: 'Generalising "easy insertion" to "always faster."', correctsTo: 'Linked lists are faster for frequent INSERTIONS (especially at head/tail). Arrays are faster for RANDOM ACCESS by index (O(1) vs O(n)). Modern hardware also favors arrays because of CACHE LOCALITY — array elements are contiguous in memory; linked list nodes scattered. For most use cases (especially with modern CPUs), arrays/dynamic-arrays beat linked lists. Always: choose data structure based on dominant operation pattern, not abstract advantages.' }], estimatedMinutes: 3 },
    { id: 'recap', kind: 'recap', mustRemember: ['Array: O(1) access, O(n) insert middle.', 'Linked list: O(1) head insert, O(n) access.', 'Stack LIFO, Queue FIFO. Both O(1).', 'BST: O(log n) avg, O(n) worst. Self-balancing fixes worst.', 'Hash: O(1) avg lookup. Most common for key-value.'], estimatedMinutes: 1 },
  ],
  source: { author: 'Evelyn Learning', org: 'Evelyn', license: 'proprietary' },
  schemaVersion: 1,
};
